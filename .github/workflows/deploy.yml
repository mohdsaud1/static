# name: Deploy Static Page to Hostinger

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v3

#       - name: Copy static files to build directory
#         run: cp -r ./* dist/

#       - name: Deploy to Hostinger
#         uses: actions/deploy@v1
#         with:
#           provider: hostinger
#           hostinger_token: ${{ secrets.HOSTINGER_TOKEN }}
#           hostinger_site_id: ${{ secrets.HOSTINGER_SITE_ID }}
#           build_dir: dist
#           publish_dir: public_html

name: Deploy Static Page to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Copy static files to build directory
        run: rsync -av --exclude='dist/' --exclude='.git/' --exclude='.github/' ./ dist/

      - name: Install rsync
        run: sudo apt-get install rsync

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.2.0
        with:
          server: 86.38.243.193
          username: ${{ secrets.HOSTINGER_USERNAME }}
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          local-dir: dist/
          server-dir: public_html/
          passive: true
